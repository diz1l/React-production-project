
import config from './config/jest/jest.config';

export default config;

// Keep this file at project root for IDE auto-discovery (e.g. VS Code Jest).
// Source of truth is in `config/jest/jest.config.ts`.